// Soon the thread feature on assbreak

/*
//std
use std::thread;
use std::sync::mpsc;

// local
use crate::func::{restart, read_lines, string_to_static_str};
use crate::brute_force::{website::brute_website, email::brute_mail};

//extern
use colored::Colorize;
use tor_stream::*;
*/

